//class is not a object it's an template....

class className {
    constructor() {...arguments.}
}

class car {
    constructor(name, year) {
        this.name = name, this.year = year; 
    }
}
console.log(car('Vugadi', 2017));



//AGE IN YEAR
class Mithu {
    constructor(name, year) {
        this.name = name, this.year = year;
    }
    print() {
        console.log(`name: ${this.name}, email:${this.year}`);
    }
}
